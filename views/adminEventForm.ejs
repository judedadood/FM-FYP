<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <style>
    nav {
      background: #003366;
      padding: 12px 25px;
      color: #fff;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    nav .logo { font-weight: bold; font-size: 20px; }
    nav ul { list-style: none; display: flex; gap: 18px; margin:0; padding:0; }
    nav a { color:#fff; text-decoration:none; font-size:14px; }
    nav a:hover { text-decoration:underline; }
    .preview {
      width: 100%;
      max-width: 420px;
      height: 240px;
      object-fit: cover;
      border-radius: 12px;
      border: 1px solid rgba(0,0,0,0.08);
      background: #f2f2f2;
    }
  </style>
</head>
<body class="bg-light">

<nav>
  <div class="logo">Condo Management System</div>
  <ul>
    <li><a href="/admin">Admin Dashboard</a></li>
    <li><a href="/admin/events">Events</a></li>
  </ul>
</nav>

<div class="container my-5" style="max-width: 900px;">
  <h2 class="mb-3"><%= mode === "create" ? "Create Event" : "Update Event" %></h2>

  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <div class="card shadow-sm">
    <div class="card-body">
      <form method="POST"
            action="<%= mode === 'create' ? '/admin/events/new' : '/admin/events/' + event.event_id + '/edit' %>">

        <div class="row g-3">
          <div class="col-md-7">
            <div class="mb-3">
              <label class="form-label">Title *</label>
              <input type="text" name="title" class="form-control" required value="<%= event.title %>">
            </div>

            <div class="row g-3">
              <div class="col-md-6">
                <label class="form-label">Event Date *</label>
                <input type="date" name="event_date" class="form-control" required value="<%= event.event_date %>">
              </div>
              <div class="col-md-6">
                <label class="form-label">Event Time *</label>
                <input type="time" name="event_time" class="form-control" required value="<%= event.event_time %>">
              </div>
            </div>

            <div class="mt-3">
              <label class="form-label">Description</label>
              <textarea name="description" class="form-control" rows="4"><%= event.description %></textarea>
            </div>

            <div class="mt-3">
              <label class="form-label">Image URL</label>
              <input type="text" name="image_url" class="form-control" value="<%= event.image_url %>"
                     placeholder="https://...">
              <div class="form-text">
                Paste an online image URL. (If empty, a placeholder will be used.)
              </div>
            </div>

            <div class="mt-4 d-flex gap-2">
              <button class="btn btn-success" type="submit">
                <%= mode === "create" ? "Create Event" : "Save Changes" %>
              </button>
              <a class="btn btn-secondary" href="/admin/events">Cancel</a>
            </div>
          </div>

          <div class="col-md-5">
            <div class="fw-semibold mb-2">Image Preview</div>
            <img class="preview"
                 src="<%= event.image_url ? event.image_url : 'https://via.placeholder.com/600x350?text=Event' %>"
                 onerror="this.src='https://via.placeholder.com/600x350?text=Event';"
                 alt="preview">
          </div>
        </div>

      </form>
    </div>
  </div>
</div>

</body>
</html>
