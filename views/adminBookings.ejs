<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <style>
    nav {
      background: #003366;
      padding: 12px 25px;
      color: #fff;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    nav .logo { font-weight: bold; font-size: 20px; }
    nav ul { list-style: none; display: flex; gap: 18px; margin:0; padding:0; }
    nav a { color:#fff; text-decoration:none; font-size:14px; }
    nav a:hover { text-decoration:underline; }
  </style>
</head>
<body class="bg-light">

<nav>
  <div class="logo">Condo Management System</div>
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/admin">Admin Dashboard</a></li>
    <li><a href="/admin/bookings">Bookings</a></li>
    <li><a href="/admin/maintenance">Maintenance</a></li>
  </ul>
</nav>

<div class="container my-5">
  <h2 class="mb-4">Recent Facility Bookings</h2>

  <table class="table table-striped table-bordered">
    <thead class="table-dark">
      <tr>
        <th>#</th>
        <th>Facility</th>
        <th>Resident</th>
        <th>Unit</th>
        <th>Contact</th>
        <th>Date</th>
        <th>Time Slot</th>
        <th>Created At</th>
      </tr>
    </thead>
    <tbody>
      <% if (bookings && bookings.length > 0) { %>
        <% bookings.forEach((b, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= b.facility_name %></td>
            <td><%= b.resident_name %></td>
            <td><%= b.unit_number %></td>
            <td><%= b.contact_number %></td>
            <td><%= b.booking_date.toISOString().slice(0,10) %></td>
            <td><%= b.time_slot %></td>
            <td><%= b.created_at.toISOString().slice(0,19).replace('T',' ') %></td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="8" class="text-center">No bookings found.</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

</body>
</html>
