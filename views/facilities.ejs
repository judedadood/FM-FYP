<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facilities</title>

    <style>
        * { box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: #f4f4f4; margin: 0; }

        nav {
            background: #003366;
            padding: 15px 25px;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        nav .logo { color: #fff; font-size: 22px; font-weight: bold; }
        nav ul { display: flex; gap: 20px; list-style: none; margin: 0; padding: 0; }
        nav ul li a { color: #fff; text-decoration: none; font-size: 15px; }
        nav ul li a:hover { text-decoration: underline; }

        .hero {
            position: relative;
            height: 260px;
            background-image: url("https://images.adsttc.com/media/images/5b67/0be1/f197/cc32/a300/002a/large_jpg/hanhai_qingyu_2.jpg?1533479842");
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: #fff;
        }
        .hero::before {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.45);
        }
        .hero-content { position: relative; max-width: 800px; padding: 0 20px; }
        .hero-content h1 { font-size: 36px; margin-bottom: 8px; }
        .hero-content p { margin: 0; font-size: 16px; }

        .page { max-width: 1150px; margin: 30px auto 60px; padding: 0 20px 40px; }
        .section-title { font-size: 24px; font-weight: bold; color: #003366; margin-bottom: 15px; }

        .info-box {
            background: #fff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
            margin-bottom: 30px;
        }
        .info-item {
            background: #e9f2ff;
            padding: 12px 18px;
            border-radius: 6px;
            margin-bottom: 10px;
            font-size: 14px;
        }
        .info-item:last-child { margin-bottom: 0; }

        .facility-grid { display: flex; flex-wrap: wrap; gap: 24px; }
        .facility-card {
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
            flex: 1 1 300px;
            max-width: 340px;
            display: flex;
            flex-direction: column;
        }
        .facility-card img { width: 100%; height: 190px; object-fit: cover; }
        .facility-body { padding: 16px 18px 18px; display: flex; flex-direction: column; gap: 8px; }
        .facility-title { font-size: 17px; font-weight: bold; color: #003366; }
        .facility-desc { font-size: 14px; color: #444; line-height: 1.4; }

        .btn-primary {
            margin-top: 6px;
            align-self: flex-start;
            border: none;
            background: #003366;
            color: #fff;
            padding: 8px 16px;
            border-radius: 20px;
            cursor: pointer;
            font-size: 13px;
            text-decoration: none;
            display: inline-block;
        }
        .btn-primary:hover { background: #00509e; }

        @media (max-width: 768px) {
            .hero { height: 220px; }
            .hero-content h1 { font-size: 28px; }
        }
    </style>
</head>
<body>

<nav>
  <div class="logo">Condo Management System</div>
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/announcements">Announcements</a></li>
    <li><a href="/facilities">Facilities</a></li>
    <li><a href="/maintenance-request">Maintenance Request</a></li>
    <li><a href="/resident/payments">Payments</a></li>
    <li><a href="/logout">Logout</a></li>
  </ul>
</nav>

<section class="hero">
    <div class="hero-content">
        <h1>Our Condo Facilities</h1>
        <p>
            Enjoy a range of shared facilities designed for relaxation, fitness and community bonding.
            Check availability and book your preferred facility through the management office or resident portal.
        </p>
    </div>
</section>

<main class="page">

    <!-- ALL AVAILABLE FACILITIES (from DB) -->
    <section>
        <div class="section-title">All Available Facilities</div>
        <div class="info-box">
            <% if (facilities && facilities.length > 0) { %>
                <% facilities.forEach(f => { %>
                    <div class="info-item">
                        <strong><%= f.name %>:</strong> <%= f.description %>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="info-item">No facilities found. Please check back again later.</div>
            <% } %>
        </div>
    </section>

    <!-- AVAILABLE FACILITIES CARDS (from DB) -->
    <section>
        <div class="section-title">Available Facilities</div>

        <div class="facility-grid">
            <% if (facilities && facilities.length > 0) { %>
                <% facilities.forEach(f => { %>
                    <div class="facility-card">
                        <img
                          src="<%= f.image_url ? f.image_url : 'https://via.placeholder.com/600x350?text=Facility' %>"
                          alt="<%= f.name %>">
                        <div class="facility-body">
                            <div class="facility-title"><%= f.name %></div>
                            <p class="facility-desc"><%= f.description %></p>
                            <a href="/facilities/book/<%= f.facility_id %>" class="btn-primary">Book Appointment</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No facilities available.</p>
            <% } %>
        </div>
    </section>

</main>

</body>
</html>
